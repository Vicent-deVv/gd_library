[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gd_api"
version = "0.1.0"
description = "Library/API for geometry dash"
dependencies = [
    requests==2.34.2
]

[tool.setuptools.packages.find]
where = ["."]